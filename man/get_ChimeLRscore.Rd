% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_ChimeLRscore.R
\name{get_ChimeLRscore}
\alias{get_ChimeLRscore}
\title{Step 2: Build a ligand–receptor (LR) table and compute LR scores}
\usage{
get_ChimeLRscore(
  combined,
  direction = c("mouse_ligand_human_receptor", "human_ligand_mouse_receptor"),
  db_scope = c("secreted", "contact", "ecm"),
  rename_to_mouse = FALSE,
  ortholog_map = NULL,
  rename_mode = c("compact", "one")
)
}
\arguments{
\item{combined}{A numeric matrix (rows = human gene symbols) returned by \code{run_ChimeDB()}.}

\item{direction}{Direction of scoring: \code{"mouse_ligand_human_receptor"} or
\code{"human_ligand_mouse_receptor"}.}

\item{db_scope}{Character vector of any of \code{c("secreted","contact","ecm")} to select
CellChatDB categories.}

\item{rename_to_mouse}{Logical; if \code{TRUE}, convert the mouse-side gene names for display.}

\item{ortholog_map}{A list or data.frame used to map human-to-mouse names (typically
\code{run_ChimeDB()$ortholog_map}).}

\item{rename_mode}{Display mode when multiple orthologs exist: \code{"one"} (first only) or
\code{"compact"} (first plus “+n more”).}
}
\value{
A tibble with columns:
\describe{
\item{direction}{Scoring direction.}
\item{ligand}{Ligand gene symbol.}
\item{receptor}{Receptor gene symbol.}
\item{score}{Sum of VST-transformed ligand and receptor values.}
\item{condition}{Condition label (Control/Treatment).}
\item{sample}{Sample identifier.}
\item{pair_human}{Pair shown with human symbols on both sides.}
\item{pair}{Display pair, with one side renamed to mouse symbols if requested.}
}
}
\description{
Select LR interactions from CellChatDB and compute LR scores as the sum of
VST-transformed ligand and receptor values for matched samples.
}
